# ==================== 外置金融大脑 敏感物 / 数据 ====================
# 站点配置本体（端点 / gate 头 / 字段样例）是逆向侦查的成果，进版本库；
# 会话 cookie（CapitalIQ 付费订阅登录态、雪球 xq_a_token）剥离到 sidecar，绝不提交。
# 剥离逻辑在 acquisition/browser/registry.py，由 tests/test_scraper_secrets_split.py 守着。
backend/acquisition/crawler/sites/*.secrets.json
# Playwright 持久化浏览器会话（cookie/登录态）
backend/data/browser_profile/
# 知识库数据（向量/全文，本地数据不入库）
backend/data/knowledge.db
backend/data/knowledge.db-*

# ==================== Python ====================
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
eggs/
*.egg-info/
*.egg
venv/
env/
.venv

# ==================== 源码全部纳入版本控制（2026-07-09）====================
# 此前策略/信号、爬虫/代理、风控、报告引擎、docs 都被当「核心机密」排除在版本库外，
# 导致大重构的「按域 commit → 出问题 revert」安全模型对它们完全失效（report_engine
# 6796 行、daily_updater 1073 行、全部 docs 都没有历史可回滚）。
# 本仓库是私有仓，源码全部纳入版控；**只排除真正的密钥与运行时产物**（见下）。

# 运行时产物 / 本机状态（不是源码）
backend/scripts/data
backend/scripts/proxy_pool.json
backend/scripts/proxy_raw_responses/
backend/scripts/*_progress.json
backend/scripts/*_progress.json.bak.*
backend/scripts/etf_list_cache.json
.claude/
frontend/.claude/
.vscode/

# 第三方参考仓库（零件仓库，非本项目代码）
TradingAgents-main/

# ==================== 数据 & 数据库 ====================
*.db
*.db.backup
*.sqlite
*.sqlite3
backend/data/

# ==================== CTP 交易通信 ====================
backend/md_flow/
backend/td_flow/

# ==================== 测试文件 ====================
# 全部纳入版本控制。打真实网络/连本地服务的用例靠 pytest marker 隔离
# （backend/pyproject.toml 的 addopts 默认 -m 'not network and not integration'），
# 不再靠 gitignore 白名单——那套 deny-by-default 让新写的测试默默不进版本库。
backend/tests/**/__pycache__/
backend/tests/**/.pytest_cache/

# ==================== 环境变量 & 密钥 ====================
.env
.env.*
*.key
*.pem
backend/.env.example
# 例外：只含一行公开的构建期 API 基址（无密钥），Tauri 打包桌面版靠它把
# VITE_API_URL 覆盖成绝对地址，不追踪的话打包修复在新机器/clone 后会静默失效。
!frontend/.env.production

# ==================== 部署脚本 ====================
# restart.sh 是开发脚本、无密钥，2026-07-09 起纳入版本控制

# ==================== 日志 ====================
logs/
# backend/logs 已挪到 T9 外置盘、现在是软链接，trailing-slash 规则不匹配软链接，故单独列出
backend/logs
*.log
*.log.*
nohup.out

# ==================== 进度 & 临时文件 ====================
*_progress.json
*.tmp
*.temp
*.bak
*.cache
*.csv
exports/

# ==================== IDE ====================
.vscode/
.idea/
*.code-workspace
*.iml
.ipynb_checkpoints/
*.swp
*.swo
*~

# ==================== 操作系统 ====================
.DS_Store
._*
Thumbs.db
Desktop.ini

# ==================== 前端 ====================
node_modules/
frontend/build/
frontend/dist/
coverage/
package-lock.json
yarn.lock
pnpm-lock.yaml

# ==================== 远程训练 ====================
server_finetune/data/
server_finetune/output/
backend/finetune/remote_output/

# ==================== 其他 ====================
.pytest_cache/
.coverage
htmlcov/
*.zip
*.tar.gz
*.o
*.a
*.exe
*.out
code_dataset/
datasets/
TradingAgents-main/

.steploop/
