"""市场无关的策略执行运行时（S5 批次2）。

## 定位

一条策略从「规则」走到「成交」要经过同样的几步，跟标的是股票还是币无关：

```
tick → 分析卡 → DSL 评估 → 交易单位换算 → 可交易性 → 🔒 硬风控 → 下单 → 留痕
```

市场之间**不同**的只有三样：分析卡从哪来、下单走哪个 broker、交易单位/结算规则。
这三样收在 `MarketAdapter` 里，其余全是共享的。

## ⚠️ 过渡态：现在有两个执行器

`crypto_strategy/engine.py` 是**正在跑真钱**的 crypto 引擎，它跟币安的成本闸
（funding / taker / 实测滑点）耦合较深。本运行时**先只服务股票**，
crypto 迁移过来是**明确的欠债**（S5 批次3 或之后）。

⛔ 这不是「两套并行」的许可证。Jason 2026-08-02 拍板的是「共用一套，不另建」，
现在的两份是为了**不在同一次改动里重写实盘链路**而做的临时妥协。
⚠️ 往这里加能力时，先想想 crypto 那边是不是也该有 —— 别让两边越漂越远。
"""
