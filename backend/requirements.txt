# Web Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.13.4              # 2.5.3→2.13.4：langgraph 1.x 依赖顶上来（全量测试 879 passed 验证兼容，见 13.5 阶段A）
pydantic-settings==2.1.0
python-jose[cryptography]==3.5.0

# Database
sqlalchemy==2.0.25
alembic==1.13.1

# Data Sources
akshare>=1.18.0
yfinance>=0.2.35
tushare>=1.3.0

# Data Processing
pandas==2.1.4
numpy==1.26.3

# Task Scheduling
apscheduler==3.10.4

# Logging
loguru==0.7.2

# Utils
python-dotenv==1.0.0
tenacity==8.2.3
requests==2.31.0

# Trading
openctp-ctp>=6.7.2

# AI
openai>=1.0.0
duckduckgo-search>=6.0.0

# 新闻模块
finnhub-python>=2.4.0
transformers>=4.40.0
torch>=2.0.0

# 外置金融大脑 knowledge_engine（RAG 向量检索 + 本地 embedding + 联网摄入）
sqlite-vec>=0.1.9            # vec0 虚拟表 + serialize_float32（向量存进 knowledge.db）
sentence-transformers>=5.6.0 # 本地 embedding 模型 bge-m3 加载（零 API 成本）
trafilatura>=2.1.0          # 网页正文去噪提取（read_url HTML 路径）
PyMuPDF>=1.27.2             # PDF 正文抽取（read_url PDF 路径，import 名 fitz）
curl_cffi>=0.13.0          # DDG 搜索的 TLS 指纹伪装
beautifulsoup4>=4.12.0      # HTML 解析（DDG 结果 + read_url 回退）
playwright>=1.40           # 逆向 API 爬虫：无头浏览器过盾 + 嗅探 gate 头（装后需 playwright install）
lxml>=5.0.0                 # bs4 解析器后端

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3

# LangGraph（薄用四原语：StateGraph + sqlite checkpointer + interrupt + subgraph，见 CODING_STANDARDS §6）
# 阶段A：alpha_lab 深任务迁循环子图 + 断点续跑；GRAPH_ALPHA_LAB=off 默认回落旧引擎
langgraph==1.2.9
langgraph-checkpoint-sqlite==3.1.0

# Lint / Type check（配置见 backend/pyproject.toml，规范见 docs/CODING_STANDARDS.md §10）
ruff>=0.15
mypy>=1.8
