# 生产构建（vite build → Tauri 桌面壳 frontendDist: "../dist"）专用。
# 打包版没有 vite dev proxy，相对路径 /api 是死路；桌面端始终访问本机稳定后端（app 端，8000 端口，不带 --reload），
# 因此把 API 基址覆盖成绝对地址。dev（vite，走 8010 web 开发后端）仍用 .env 里的 /api 走 proxy。
VITE_API_URL=http://127.0.0.1:8000
