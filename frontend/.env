# 前端只服务 Mac 桌面 App（2026-07-21 收敛，web 端已退役）。
# App 加载的是 tauri build 打进包里的静态产物，没有 vite dev proxy 可依赖，
# 相对路径 /api 是死路 —— 一律用绝对地址直连本机后端。
# vite 在 dev 和 build 两种模式都会读这个文件，所以一份就够（原 .env.production 已删）。
VITE_API_URL=http://127.0.0.1:8000
